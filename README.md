# HedronDB

Local-first knowledge OS for agents.

Phase 0 lives in `hedron-core` (rusqlite, Desired State + causal Event Log, Option B recon). Tests are the surface. No HTTP, no Python/HQL yet.

Store files must be mode 0600.
